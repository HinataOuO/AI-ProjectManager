# Maintainer

Il package contiene solo primitive generiche: `core/`, `skills/` e template. I fatti del progetto vanno in `.pi/ai-project/local/project/`, mai nel runtime aggiornabile.

`runtime` viene sostituito in modo atomico; `.pi/skills/` è una proiezione rigenerabile delle skill runtime per l'auto-discovery Pi.
